select * from Language_sys_tab t
where t.type = 'Basic Data' and 
lang_code = 'PROG' and module = upper('&module')

delete prog text related to task title

import language file

goto scan translatable code add LU name to scan. (can find lu name from lng by searching for task package name)

set context obsolete for selected lu by using 2nd tab

scan above LU. export lng will have new titles.